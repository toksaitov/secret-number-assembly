.section .text
.global throw_random_points_into_circle_with_assembly
throw_random_points_into_circle_with_assembly:
    push %rbp
    mov %rsp, %rbp

    # TODO: Recreate the `throw_random_points_into_circle` function from the previous task here in assembly.

    leave

    ret
